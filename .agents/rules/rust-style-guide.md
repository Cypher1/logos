---
trigger: always_on
---

1. You can always use `cargo check`.
2. You can always use `ls`, `grep`, `git grep` and `git ls-files`.
3. This project is in Rust, and uses ideas from programming languages, AI, and type theory.
4. Notes are kept in markdown files (README.md, TODO.md, etc.).
5. The project should be split into minimal, reusable modules.